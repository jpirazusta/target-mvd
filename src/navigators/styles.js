import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  backButtonMargin: {
    marginLeft: 18,
  },
  rightButton: {
    width: 30,
    marginRight: 10,
    alignItems: 'center',
  },
});

export default styles;
