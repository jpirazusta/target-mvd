import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  regular: {
    fontWeight: 'normal',
  },
  semibold: {
    fontWeight: '600',
  },
  bold: {
    fontWeight: 'bold',
  },
});

export default styles;
