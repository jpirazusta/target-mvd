import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  image: {
    position: 'absolute',
    width: 484,
    height: 343,
    left: -13,
    top: -60,
  },
});

export default styles;
